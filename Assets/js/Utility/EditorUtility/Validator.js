Centauri.Utility.EditorUtility.Validator = function(form) {
    // const inputs = [];

    // $form = $(form, $editor);

    // $("input", $form).on("focus", function() {
    //     var id = $(this).attr("id");
        
    // });
};
